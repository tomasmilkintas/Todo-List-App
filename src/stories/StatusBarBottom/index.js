import React from "react";

import StyledStatusBarBottom from "./component";

const StatusBarBottom = (props) => {
    return <StyledStatusBarBottom {...props} />;
};

export default StatusBarBottom;
