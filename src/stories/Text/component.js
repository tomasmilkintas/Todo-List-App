import styled from "styled-components";

const StyledText = styled.p`
    text-align: center;
    font-size: 1.1em;
    padding: 0px 20px;
    height: 26px;

    &.profile {
        font-size: 1.43em;
    }
`;

export default StyledText;
