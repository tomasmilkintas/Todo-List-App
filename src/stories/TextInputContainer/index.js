import React from "react";

import StyledTextInputContainer from "./component";

const TextInputContainer = (props) => {
    return <StyledTextInputContainer {...props} />;
};

export default TextInputContainer;
